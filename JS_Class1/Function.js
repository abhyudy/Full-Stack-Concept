// Write a function that find the sum of two no.

function sum(a, b) {
  //do things with the input and return an output
  const sumValue = a + b;
  return sumValue;
}

const value = sum(1, 2);
console.log(value);

